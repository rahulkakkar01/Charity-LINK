import 'package:flutter/material.dart';

class NGOUserPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("NGO User Page")),
      body: Center(child: Text("Welcome to NGO User Page")),
    );
  }
}
